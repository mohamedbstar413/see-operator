apiVersion: see-operator.example.com/v1
kind: SeeOperator
metadata:
  labels:
    app.kubernetes.io/name: see-op
    app.kubernetes.io/managed-by: kustomize
  name: seeoperator-sample
spec:
  # TODO(user): Add fields here
